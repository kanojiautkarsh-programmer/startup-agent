import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Access Neural Workspace | TaskLyne",
  description: "Initialize your startup intelligence today. Secure premium operational clarity."
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
